# LogicXGNN

Oversmoothing kills graph structure

So we want to model common graph patterns in graph structures

A receptive (ego)-graph that is isomorphic to another will correspond to the same class.

---

See beginning of chapter 3.3:

> a predicate pj encodes a subgraph centered on a node v, encompassing its 1, . . . , L-hop neighborhoods.

NOTE for my impl, need to make sure to only use 3-4 hop neighborhoods!!

---

